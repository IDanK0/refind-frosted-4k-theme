# source photograph, fetched by build.py
dune-src.jpeg

# preview renders produced during a build
mockup*.png
mock_*.png
small_*.png
confronto*.png
preview*.png

__pycache__/
*.pyc

# local scratch: renders and disk images made while working on the theme,
# never part of a release
library/.preview/
vm/

# the rEFInd source tree and binary build-refind.sh makes
build/

# artwork build.py and plymouth.py generate. Not tracked: it is derived from
# whichever photograph you choose, it is 18 MB, and a checked-in copy is a copy
# that goes stale the first time the generator changes, which it did.
assets/
plymouth/
